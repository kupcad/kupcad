pub const Config = struct {
    check_negative_dims: bool = true,
    check_unused_vars: bool = true,
    check_unreachable_code: bool = true,
    check_self_subtraction: bool = true,
    check_param_docs: bool = true,
};
