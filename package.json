{
  "name": "kupcad",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build:core:wasm": "cd core && zig build -Dtarget=wasm32-freestanding -Doptimize=ReleaseFast",
    "build:core:native": "cd core && zig build -Doptimize=ReleaseFast",
    "test:core": "cd core && zig build test --summary all"
  }
}
