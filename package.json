{
  "name": "kupcad",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build:core:wasm": "cd core && zig build -Dtarget=wasm32-wasi -Doptimize=ReleaseSmall",
    "build:core:native": "cd core && zig build -Doptimize=ReleaseFast",
    "build:core:grammar": "cd core && zig build grammar -Doptimize=ReleaseFast",
    "test:core": "cd core && zig build test -Doptimize=Debug --summary all",
    "test:wasm": "cd core && zig build test-wasm -Dtarget=wasm32-wasi -Doptimize=Debug --summary all"
  }
}
