{
  "name": "kupcad",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build:core:wasm": "cd core && zig build -Dtarget=wasm32-wasi -Doptimize=small",
    "build:core:wasm:threads": "cd core && zig build -Dtarget=wasm32-wasi -Doptimize=small -Dwasm_threads=true",
    "build:core:native": "cd core && zig build -Doptimize=fast",
    "build:core:grammar": "cd core && zig build grammar -Doptimize=fast",
    "test:core": "cd core && zig build test -Doptimize=debug --summary all",
    "test:wasm": "cd core && zig build test-wasm -Dtarget=wasm32-wasi -Doptimize=debug --summary all",
    "test:wasm:threads": "cd core && zig build test-wasm -Dtarget=wasm32-wasi -Doptimize=debug -Dwasm_threads=true --summary all"
  }
}
